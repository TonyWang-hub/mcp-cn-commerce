name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Allow other workflows (e.g. release) to require this suite as a gate.
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio pytest-cov httpx mcp

      - name: Run server tests
        run: |
          python -m pytest servers/ -v --tb=short

      - name: Run automation and shared tests
        run: |
          python -m pytest tests/ -v --tb=short

      - name: Generate test report
        if: always()
        run: |
          python -m pytest servers/ tests/ -v --tb=short --junitxml=test-results.xml || true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-py${{ matrix.python-version }}
          path: test-results.xml

  # Build the wheel, install it into a clean venv, and exercise the INSTALLED
  # package against the real MCP SDK. Catches broken packaging and modules
  # that only import under mocked tests (both shipped in 0.1.x releases).
  install-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Build wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build --wheel

      - name: Install into clean venv and run smoke test
        run: |
          python -m venv "$RUNNER_TEMP/smoke-venv"
          source "$RUNNER_TEMP/smoke-venv/bin/activate"
          pip install dist/*.whl
          cd "$RUNNER_TEMP"
          python "$GITHUB_WORKSPACE/scripts/smoke_install.py"

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install black ruff

      - name: Check code formatting
        run: |
          black --check .

      - name: Check code style
        run: |
          ruff check .

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mypy types-requests httpx mcp

      - name: Run mypy type checking
        run: |
          mypy servers/ shared/ tests/ --config-file=pyproject.toml

  code-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pylint bandit[toml] mcp httpx

      - name: Run pylint
        run: |
          pylint --rcfile=pyproject.toml --disable=import-error servers/ shared/

      - name: Run bandit security check
        run: |
          bandit -c pyproject.toml -r servers/ shared/
