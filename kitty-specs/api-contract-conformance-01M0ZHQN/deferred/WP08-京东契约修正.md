---
work_package_id: WP08
title: 京东契约修正
dependencies:
- WP02
requirement_refs:
- FR-009
tracker_refs: []
planning_base_branch: sdd/api-contract-conformance
merge_target_branch: sdd/api-contract-conformance
branch_strategy: Planning artifacts for this mission were generated on sdd/api-contract-conformance. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into sdd/api-contract-conformance unless the human explicitly redirects the landing branch.
subtasks:
- T021
- T022
phase: Phase 2 - Platforms
assignee: ''
agent: ''
history:
- timestamp: '2026-08-26T17:37:08Z'
  agent: system
  action: Prompt generated via /spec-kitty.tasks
authoritative_surface: servers/jd/
create_intent:
- tests/contract/test_wire_jd.py
- docs/api-contracts/jd.md
execution_mode: code_change
owned_files:
- servers/jd/server.py
- tests/contract/test_wire_jd.py
- docs/api-contracts/jd.md
- servers/jd/tests/test_jd.py
tags: []
---

# Work Package Prompt: WP08 – 京东契约修正

实现 FR-009。`servers/jd/server.py` 覆写了 `_sign` 但走 base `_request`。

## endpoint 审计结果与替代映射（已验证，勿重新调研）

判据：官方文档后端拉取**完整目录 3514 个 API**（覆盖三个网关），做前缀直方图与逐个存在性验证。

### `jd.pop.*` 命名空间不存在 —— 15/15 全部编造

| 前缀 | 数量 | 说明 |
|---|---|---|
| `jingdong.` | **3147** | 全部商家/POP/VC 接口（1.0 网关） |
| `jd.union.open.` | 90 | **仅**京东联盟 CPS |
| `jd.pop.` | **0** | 不存在 |
| 无点分裸名（`listOrders` 等） | ~277 | SP-API |

`jd.` 是真前缀但只属联盟 —— 几乎可确定是污染源：从 `jd.union.open.order.query` 推断出 `jd.<域>.<资源>.<动作>` 语法后生成了 `jd.pop.*`。

| 我们调用的 | 官方正确 method | 权限 |
|---|---|---|
| `jd.pop.order.search` | **`jingdong.pop.order.search`**（apiId 4246, group 55, 2026-08-18 更新） | R3 |
| `jd.pop.order.get` | **`jingdong.pop.order.get`**（2026-08-18 更新） | R3 |
| `jd.pop.ware.search` | `jingdong.ware.read.searchWare4Valid` | — |
| `jd.pop.shop.get` | `jingdong.vender.shop.query` | R1 |
| `jd.pop.afs.search` | `jingdong.ServiceInfoProvider.queryServicePageSafe` 或 `jingdong.afsservice.alltask.get` | R1 |
| `jd.pop.afs.get` | `jingdong.ServiceDetailProvider.findServiceDetail` | R1 |
| `jd.pop.comment.search` | `jingdong.pop.PopCommentJsfService.getVenderCommentsForJos` | R1 |
| `jd.pop.inventory.get` | `jingdong.ware.stock.sku.query` | R1 |
| `jd.pop.promotion.search` | `jingdong.seller.promotion.list` | R1 |
| `jd.pop.coupon.search` | `jingdong.seller.coupon.read.getCouponList` | R1 |
| `jd.pop.category.search` | `jingdong.category.read.findByPId`（对应我们的 `parent_id`） | — |

**4 个无官方等价能力**：`jd.pop.shop.score.get`（3514 个 API 中 DSR/店铺评分/口碑/服务分零命中）、`jd.pop.comment.get`（评价 API 仅 6 个，无单条详情）、`jd.pop.price.get`（无 POP 实时售价读接口，价格随 `jingdong.ware.read.findWareById` 返回）、`jd.pop.logistics.trace`（无单一等价；`jingdong.ldop.receive.trace.get` 需青龙业主号+运单号）。

### ⚠️ 需先决策：routerjson 还是 SP-API

官方《开放平台API对接指南》明文把 `https://api.jd.com/routerjson` 标为「**历史接口，逐步迁移至SP-API**」，`https://api-cn.jd.com` 的 **SP-API 标为「新一代标准化接口（推荐使用）」**（19 组约 277 个 endpoint）。POP 所需能力 SP-API 已全覆盖：

| 能力 | SP-API | 权限 |
|---|---|---|
| 订单列表 | `GET /rest/sp-order/v0/orders`（`listOrders`） | R3 |
| 订单详情 | `GET /rest/sp-order/v0/orders/{orderId}` | R3 |
| 物流轨迹 | `GET /rest/sp-order/v0/waybill-traces` | R1 |
| 商品/SKU/库存 | `listProducts` / `listSkus` / `listSkuStocks` | R1 |
| 售后 | `listAfsOrders` / `getAfsOrder` | R2 |
| 店铺信息 | `getVenderInfo` | R1 |

**SP-API 鉴权完全不同**：凭证走 header（`X-JOS-App-Key`、`X-JOS-Access-Token`、`X-JOS-Timestamp` 为 epoch **毫秒**、`X-JOS-Sign`），签名 `upper(md5(secret + sorted_kv + secret))`，且 **header 名本身参与签名串**。

→ **在决定网关之前不要动手**。若选 SP-API，本 WP 的 routerjson 契约细节（签名向量等）全部作废；若选 routerjson，等于在官方标注的历史接口上重建。

### 机读判据

```bash
# 存在性 oracle：返回完整元数据，或 {"msg":"API不存在","bCode":"6031"}
curl -s -X POST "https://sff.jd.com/api?v=1.0&appId=ANVFWIKZWFE8M1GZ3QU5&api=dsm.jdo.open.cms.api4home.detail4Api" \
  -H "Content-Type: application/json" -H "Referer: https://open.jd.com/" -H "dsm-platform: pc" \
  -d '{"request":{"data":{"apiName":"jingdong.pop.order.search","gwType":0}}}'
# gwType: 0=1.0/routerjson  1=SP-API  2=SPI
```

**注意**：目录**无法区分「已下线」与「从未存在」**（都返回 `API不存在`）；`apiStatus` 100% 为 1、`apiSuspend`/`apiState` 100% 为 null，退役接口是直接删除。京东标注废弃的方式是**写在中文名里**（如"【推下线】…【请切换到 jingdong.ware.stock.sku.set接口】"，共 30 条），可机读。

### 尚缺

上述 11 个 `jingdong.*` 的请求/响应字段契约、以及 SP-API 的详细契约，均未调研。

## 官方契约 vs 现状

| 项 | 官方（签名已复现验证） | 现状 |
|---|---|---|
| timestamp | `yyyy-MM-dd HH:mm:ss`，或带毫秒时区 `.SSSZ`（如 `2021-05-07 09:20:39.683+0800`）。**容差 6 分钟**（不是 10） | epoch 毫秒 |
| 签名 | `UPPER(MD5(appSecret + Σ(name+value, ASCII升序) + appSecret))` —— secret 首尾各一次 | **HMAC-MD5** |
| 业务参数 | 放 `360buy_param_json`（一个 JSON 字符串参数），**参与签名**且因 `3` 开头排最前 | 放 POST JSON body，**不参与签名** |
| `format` | 官方参数表写"必传"，但**已验证的签名串里没有它**；京东健康版文档写"否" | 发了**且被签了**（`jd._call` 放进 params，经 `:2898` 合入后在 `:2900` 入签）→ 与官方向量不一致 |
| `sign_method` | 不在公共参数表，md5 默认不传 | 发了**且没入签**（`:2901` 在算完 sign 之后才加）—— 双重不符 |
| 错误信封 | `error_response.{code, zh_desc, en_desc}` | 读 `msg` → **所有京东报错显示 "unknown"** |
| `access_token` | 普通参数（非 header），参与签名，非授权接口整个省略 | ✅ 名字和位置正确 |
| 网关 | `https://api.jd.com/routerjson`（仅 HTTPS） | ✅ |

## 官方验证向量（WP01 已固化）

官方示例拼接串 → `D70825340F4084360B9362B60DFD7930`。该向量同时坐实：secret 首尾包裹、`360buy_param_json` 参与且排最前、`access_token` 参与、`format` 与 `sign_method` **不参与**、timestamp 用原文字符串、结果大写。

## 注意

- **只实现 md5。** hmacmd5/hmacsha256 的官方期望值穷举 100+ 变体均无法复现（文档数据陈旧），按 spec.md §8 不实现。
- **timestamp 显式带 `+0800`**，不依赖进程时区 —— 官方 Java SDK 用 JVM 默认时区，容器跑 UTC 会有 8 小时偏差直接报错码 8。MCP server 常在容器运行，这条必须落实。
- `360buy_param_json` **只序列化一次并缓存该字符串**，签名和发送用同一对象。官方明文要求「拼接过程中 param_json 里的参数顺序应与请求传入顺序一致」。
- 官方 SDK 的做法是系统参数走 query、`360buy_param_json` 走 `application/x-www-form-urlencoded` 的 POST body，规避 URL 长度上限。建议照此。
- 响应字段**按名取，不要按位置** —— 错误码 62 的官方示例字段顺序是 `zh_desc,en_desc,code`。另需兼容 token 接口的 `{code,msg,requestId}` 信封。
- 待评估（out of scope）：官方将 `routerjson` 标注为"历史接口，逐步迁移"，新网关 SP-API `https://api-cn.jd.com/rest` 走 `X-JOS-*` header 签名。

## 通用约束

1. 签名参与集合必须恒等于发送集合减去 `sign`（WP02 提供通用断言，必须启用）。
2. 签名与发送使用同一字符串对象；业务参数 JSON 只序列化一次并缓存。
3. 契约声明文档中每项结论标注官方出处 URL，区分「官方明文」与「推断」。
4. 官方缺失/矛盾项按 spec.md §8 处理，不得推测填充。
5. 交付需附 wire 层断言：参数名集合、传输位置、timestamp 格式。
