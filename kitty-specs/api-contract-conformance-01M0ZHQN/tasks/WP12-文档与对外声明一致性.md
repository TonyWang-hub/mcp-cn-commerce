---
work_package_id: WP12
title: 文档与对外声明一致性
dependencies:
- WP03
- WP05
- WP06
- WP07
- WP08
- WP09
- WP10
- WP11
requirement_refs:
- FR-013
- FR-015
tracker_refs: []
planning_base_branch: sdd/api-contract-conformance
merge_target_branch: sdd/api-contract-conformance
branch_strategy: Planning artifacts for this mission were generated on sdd/api-contract-conformance. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into sdd/api-contract-conformance unless the human explicitly redirects the landing branch.
subtasks:
- T029
- T030
- T031
phase: Phase 3 - Documentation
assignee: ''
agent: ''
history:
- timestamp: '2026-08-26T17:37:08Z'
  agent: system
  action: Prompt generated via /spec-kitty.tasks
authoritative_surface: docs/
create_intent: []
execution_mode: code_change
owned_files:
- README.md
- README_en.md
- docs/platforms.md
- docs/FAQ.md
- servers/oceanengine/server.py
- servers/doudian/server.py
- servers/jd/server.py
- servers/oceanengine/tests/test_oceanengine.py
- servers/doudian/tests/test_doudian.py
- servers/jd/tests/test_jd.py
- scripts/smoke_install.py
tags: []
---

# Work Package Prompt: WP12 – 文档与对外声明一致性

实现 FR-013，覆盖 IC-09。**须待各平台实际状态确定后再改口径。**

## FR-015：移除不存在 endpoint 对应的工具（覆盖全 8 平台）

四个**保留**平台的工具删除已写入各自 WP（WP07 删 4 个、WP09 删 3 个、WP10 删 2 个 + 移出 ddk）。本 WP 负责三个**暂缓**平台（微信小店已拉回本 mission，由 WP11 处理） —— 它们的重建另立 mission，但"不能继续对外声明不存在的能力"这件事必须现在做：

| 平台 | 真实可用 | 需处理的工具数 | 处理方式 |
|---|---|---|---|
| 抖店 | 0 / 20 | 20 | 全部移除或标记不可用；其中 8 个能力平台不对三方开放（评价×2、直播×2、店铺基础信息、店铺流量、短视频数据、IM 消息），另 12 个待后续 mission 按真实路径重建 |
| 京东 | 0 / 15 | 15 | 同上；其中 4 个无官方等价能力（店铺评分、单条评价详情、实时售价、订单级物流轨迹） |
| 巨量引擎 | 2 / 18 | 16 | 保留 `2/advertiser/info/`、`2/advertiser/fund/get/` 两个工具，其余移除或标记 |

**要求**：在 `docs/platforms.md` 为每个被移除的工具记录原因，分三类标注 —— `不存在`（从未存在）/ `已下线`（附公告日期）/ `平台不对三方开放`（附是否可申请定向开放）。这份记录是后续 mission 的输入，也是对使用者的诚实交代。

**注意 `scripts/smoke_install.py`**：它硬编码了每个 server 的期望工具数（`EXPECTED_TOOLS`），工具数变化后必须同步，否则 install-smoke 会红（该 job 在 NFR-003 验收清单内）。

## 已知需更正项

| 位置 | 现状 | 问题 |
|---|---|---|
| `README.md` / `README_en.md` | `README.md:84`「所有 8 个平台全部通过」、`:363` 的能力表、147 tools / 358 tests 口径 | 与实际状态不符；修复完成后按真实覆盖度重述（注意：字面并无「8/8 全部完成」一句，勿照抄） |
| `docs/platforms.md:36` | 淘宝签名方式记为 HMAC-MD5 | 与代码（MD5）不一致。官方三种并列合法，需按实际实现记载 |
| `docs/platforms.md` 各平台签名/鉴权列 | 沿用旧假设 | 按本 mission 确定的契约重写 |
| `docs/FAQ.md` 巨量资质 | `Developer account with approved app` | 官方要求**企业认证 + 企业打款认证**，比现述严格得多 |
| `docs/FAQ.md` 平台阶段 | 淘宝/拼多多列为 Phase 2 planned、快手/小红书/微信列为 Phase 3 planned | 但代码已发布且工具已注册 —— 口径需与实际一致 |

## 补充

- 把各平台契约声明（`docs/api-contracts/*.md`）的结论沉淀进 `docs/platforms.md`，保留官方出处 URL
- issue #102（淘宝 key 申请 / 个人店）：淘宝对接修好后应回帖告知，避免提问者按旧状态配置真实凭证后得出"工具不可用"的结论
- 涉及对外表述，措辞需人工确认后再提交

## 通用约束

1. 签名参与集合必须恒等于发送集合减去 `sign`（WP02 提供通用断言，必须启用）。
2. 签名与发送使用同一字符串对象；业务参数 JSON 只序列化一次并缓存。
3. 契约声明文档中每项结论标注官方出处 URL，区分「官方明文」与「推断」。
4. 官方缺失/矛盾项按 spec.md §8 处理，不得推测填充。
5. 交付需附 wire 层断言：参数名集合、传输位置、timestamp 格式。
