# Runtime dependency lock for mcp-cn-commerce.
#
# This is a RUNTIME lock (not dev/test/build tooling). It pins the core
# runtime dependencies (mcp, httpx) and their transitive dependencies to the
# exact versions verified in the reference environment.
#
# Generated as `pip freeze` of a clean venv holding only the runtime closure
# of mcp + httpx. Regenerate the same way after changing either pin.
#
# mcp v2 pulls a notably larger closure than v1: mcp-types (pinned to the
# exact mcp version), opentelemetry-api (tracing is on by default), and the
# httpx2 stack the SDK uses in place of httpx. Our own outbound platform
# calls still use httpx, so both HTTP stacks are present by design.
#
# Use:  pip install -r requirements-lock.txt
mcp==2.1.1
mcp-types==2.1.1
httpx==0.28.1
annotated-types==0.8.0
anyio==4.15.0
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httpx2==2.12.0
idna==3.19
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
opentelemetry-api==1.44.0
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
PyJWT==2.13.0
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.6.3
sse-starlette==3.4.11
starlette==1.6.0
truststore==0.10.4
typing_extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.52.4
